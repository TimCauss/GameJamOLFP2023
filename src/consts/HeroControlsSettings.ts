//fichier de settings pour le control du héro:

enum HeroControlsSettings {
    Speed = 400,            // Changer cette valeur impact la velocité de la course
    Jump = -800,            // Changer cette valeur impact la velocité du saut

    JumpCountMax = 2,       // Changer cette valeur impact le nombre de saut consécutif maximum
}

export default HeroControlsSettings;